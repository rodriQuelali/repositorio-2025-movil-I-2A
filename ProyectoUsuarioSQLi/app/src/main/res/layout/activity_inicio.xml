<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:id="@+id/inicio"
    android:background="@drawable/bg_gradiente_inicio"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Logo o ícono superior -->
    <ImageView
        android:id="@+id/imgLogo"
        android:src="@drawable/ic_launcher_foreground"
        android:contentDescription="Logo"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginBottom="40dp"
        app:tint="@android:color/white" />

    <!-- Título de bienvenida -->
    <TextView
        android:id="@+id/tvTitulo"
        android:text="¡Bienvenido al Sistema de Gestión!"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:textAlignment="center"
        android:textSize="22sp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="50dp" />

    <!-- Botón: Gestionar Usuarios -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnGestionarUsuarios"
        android:text="👤 Gestionar Usuarios"
        android:textAllCaps="false"
        android:textSize="18sp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        style="@style/Widget.MaterialComponents.Button"
        app:cornerRadius="18dp"
        app:icon="@drawable/ic_launcher_foreground"
        app:iconPadding="12dp"
        app:iconGravity="textStart"
        android:padding="16dp"
        app:backgroundTint="@color/design_default_color_background"
        android:textColor="@color/black" />



    <!-- Botón: Gestionar Artículos -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnGestionarArticulos"
        android:text="📦 Gestionar Artículos"
        android:textAllCaps="false"
        android:textSize="18sp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        style="@style/Widget.Material3.Button"
        app:cornerRadius="18dp"
        app:icon="@drawable/ic_launcher_foreground"
        app:iconPadding="12dp"
        app:iconGravity="textStart"
        app:backgroundTint="@color/design_default_color_background"
        android:textColor="@color/black" />

</LinearLayout>
